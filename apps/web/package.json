{
  "name": "@tradepilot/web",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@tradepilot/config": "workspace:*",
    "@tradepilot/core": "workspace:*",
    "@tradepilot/types": "workspace:*"
  },
  "devDependencies": {
    "vite": "^4.5.5"
  }
}
